const sortOrder = {
    BY_TITLE: "by title",
    BY_DATE_CREATED: "by date created",
    BY_DATE_MODIFIED: "by date modified"
}

const filter = {
    BY_TIME_RANGE: "by time range"
}

export { sortOrder, filter };